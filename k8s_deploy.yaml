---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
  template:
    metadata:
      labels:
        app: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
    spec:
      containers:
      - name: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
        image: bigdata-registry.local:5043/${BRANCH_NAME}/${DOCKER_IMAGE_NAME}:${BUILD_ID}
        ports:
        - containerPort: 9000

---

apiVersion: v1
kind: Service
metadata:
  name: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
  labels:
    app: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
spec:
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: http
  selector:
    app: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingr-${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
spec:
  rules:
  - host: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}.bigdata.local
    http:
      paths:
      - path: /
        backend:
          serviceName: ${BRANCH_NAME}-${DOCKER_IMAGE_NAME}
          servicePort: 9000
---
